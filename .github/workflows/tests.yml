name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  lint:
    name: Lint & Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install linting tools
        run: pip install ruff black

      # Lint is advisory on main (production branch; code is synced from develop,
      # where lint enforcement lives). Keep it visible but non-blocking here.
      - name: Ruff (lint)
        continue-on-error: true
        run: ruff check workspace/

      - name: Black (format check)
        continue-on-error: true
        run: black workspace/ --check

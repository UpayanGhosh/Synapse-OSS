# User Features

Directory for bot-authored new features, experiments, and scratch code.

The bot has write access here via the write_file / edit_file tools.
Protected paths elsewhere (pipeline/, core modules, secrets) remain read-only.


# ============================================================
# Synapse-OSS — Core Dependencies
# Install with: pip install -r requirements.txt
#
# For channels:  pip install -r requirements-channels.txt
# For ML/NLP:    pip install -r requirements-ml.txt
# For optional:  pip install -r requirements-optional.txt
# For dev tools: pip install -r requirements-dev.txt
# ============================================================

# --- Web Framework & Server ---
fastapi>=0.104.0
uvicorn>=0.24.0
httpx>=0.25.0
requests>=2.31.0
pydantic>=2.5.0

# --- System Monitoring ---
psutil>=5.9.0

# --- Terminal UI ---
rich>=13.0.0
typer[all]>=0.24.0              # CLI framework (onboarding wizard + synapse entry point)
questionary>=2.1.0              # Interactive prompts: text, password, select, checkbox, confirm (fallback)
InquirerPy>=0.3.4               # Preferred terminal UI: better Windows compat + fuzzy search

# --- Database & Vector Search ---
sqlite-vec>=0.1.1

# --- LLM Routing ---
litellm>=1.40.0                  # Unified LLM API (Gemini, Claude, Ollama, OpenRouter dispatch)

# --- Configuration ---
PyYAML>=6.0                      # personas.yaml + language_patterns.yaml parsing

# --- Background Workers ---
schedule>=1.2.0                  # GentleWorker task scheduling
filelock>=3.12.0                 # File locking for SBS audit and profile writes

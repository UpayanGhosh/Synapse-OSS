services:
  synapse:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      # Persist databases and logs across container restarts
      - synapse_data:/root/.synapse

volumes:
  synapse_data:

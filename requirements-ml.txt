# ============================================================
# Synapse-OSS — ML / NLP Dependencies
# Install with: pip install -r requirements-ml.txt
#
# Required for local embeddings, reranking, and toxicity scoring.
# Skip if using Ollama embeddings only and disabling Toxic-BERT.
# Core deps required first: pip install -r requirements.txt
# ============================================================

# --- Embeddings ---
sentence-transformers>=2.2.0     # Fallback embedding model (all-MiniLM-L6-v2)

# Note: flashrank + flashtext moved to core requirements.txt — they are eager-imported
# by memory_engine.py + smart_entity.py on the chat path. This file only ships deps
# that load lazily (Toxic-BERT via LazyToxicScorer) or opt-in features.

# --- ML / Toxicity Scoring ---
torch>=2.0.0                     # PyTorch (Toxic-BERT inference)
transformers>=4.35.0             # Hugging Face Transformers (Toxic-BERT)

# --- Audio Transcription ---
groq>=0.4.0                      # Groq Whisper API (voice message transcription)

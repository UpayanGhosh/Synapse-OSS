---
phase: 01-foundation-config
plan: 05
type: execute
wave: 3
depends_on:
  - 01-01
files_modified:
  - workspace/change_tracker.py
  - workspace/change_viewer.py
  - workspace/finish_facts.py
  - workspace/monitor.py
  - workspace/purge_trash.py
  - workspace/sci_fi_dashboard/ingest.py
  - workspace/sci_fi_dashboard/migrate_graph.py
  - workspace/sci_fi_dashboard/persona.py
  - workspace/sci_fi_dashboard/smart_entity.py
  - workspace/sci_fi_dashboard/state.py
  - workspace/sci_fi_dashboard/verify_dual_cognition.py
autonomous: true
requirements:
  - CONF-01

must_haves:
  truths:
    - "Synapse-OSS gateway starts without ImportError or RuntimeError on a host with no openclaw binary"
    - "All DB_PATH, WORKSPACE, and config path constants in workspace/ root files and sci_fi_dashboard/ module files route through SynapseConfig.load()"
    - "state.py subprocess call to openclaw sessions list is commented out with TODO Phase 7 annotation"
  artifacts:
    - path: "workspace/sci_fi_dashboard/state.py"
      provides: "state.py with openclaw subprocess stubbed"
      contains: "TODO Phase 7"
    - path: "workspace/monitor.py"
      provides: "monitor.py with LOG_DIR from SynapseConfig"
      contains: "SynapseConfig"
  key_links:
    - from: "workspace/ root files (5)"
      to: "workspace/synapse_config.py"
      via: "from synapse_config import SynapseConfig (standard relative import)"
      pattern: "SynapseConfig\\.load\\(\\)"
    - from: "workspace/sci_fi_dashboard/ files (6)"
      to: "workspace/synapse_config.py"
      via: "try/except relative import pattern"
      pattern: "from synapse_config import SynapseConfig"
---

<objective>
Sweep the 11 Python files in `workspace/` root and `workspace/sci_fi_dashboard/` that still hardcode `~/.openclaw/` paths. All files at these two directory depths use the standard direct import: `from synapse_config import SynapseConfig`. The `state.py` file requires specific handling (subprocess stub). This plan runs in parallel with Plan 06 (scripts/ and skills/ files) — they touch disjoint file sets.

Purpose: After this plan plus Plan 06, `grep -r openclaw workspace/ --include="*.py"` returns zero results (excluding migrate_openclaw.py and tests).

Output: 11 modified Python files with `~/.openclaw/` replaced by `SynapseConfig.load()` equivalents.
</objective>

<execution_context>
@C:/Users/upayan.ghosh/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/upayan.ghosh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/upayan.ghosh/personal/Synapse-OSS/.planning/ROADMAP.md
@C:/Users/upayan.ghosh/personal/Synapse-OSS/.planning/phases/01-foundation-config/01-RESEARCH.md
@C:/Users/upayan.ghosh/personal/Synapse-OSS/.planning/phases/01-foundation-config/01-01-SUMMARY.md

<interfaces>
<!-- From workspace/synapse_config.py (Plan 01): -->
```python
from synapse_config import SynapseConfig
cfg = SynapseConfig.load()
cfg.data_root    # Path: ~/.synapse/ (or $SYNAPSE_HOME)
cfg.db_dir       # Path: data_root / "workspace" / "db"
cfg.sbs_dir      # Path: data_root / "workspace" / "sci_fi_dashboard" / "synapse_data"
cfg.log_dir      # Path: data_root / "logs"
```

<!-- Import path guidance: -->
# From workspace/*.py: from synapse_config import SynapseConfig  (direct — synapse_config.py is a sibling)
# From workspace/sci_fi_dashboard/*.py: try relative first, fallback to absolute:
#   try:
#       from synapse_config import SynapseConfig
#   except ImportError:
#       import sys as _sys, os as _os
#       _sys.path.insert(0, _os.path.abspath(_os.path.join(_os.path.dirname(__file__), "..")))
#       from synapse_config import SynapseConfig

<!-- Replacement patterns: -->
# ~/.openclaw/workspace/db/*.db → str(SynapseConfig.load().db_dir / "*.db")
# ~/.openclaw/ or ~/.openclaw → str(SynapseConfig.load().data_root)
# ~/.openclaw/workspace/sci_fi_dashboard/synapse_data → str(SynapseConfig.load().sbs_dir)
# tempfile.gettempdir() + "openclaw" → str(SynapseConfig.load().log_dir)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep openclaw paths in workspace/ root files</name>
  <files>
    workspace/change_tracker.py
    workspace/change_viewer.py
    workspace/finish_facts.py
    workspace/monitor.py
    workspace/purge_trash.py
  </files>
  <action>
For each of the 5 files in `workspace/` root, apply the following substitution pattern. Read each file fully before editing.

**Import addition (top of file, after existing imports):**
```python
from synapse_config import SynapseConfig
```
Do NOT use sys.path tricks for these files — `synapse_config.py` is a sibling in the same directory.

**Standard substitutions:**
- `os.path.expanduser("~/.openclaw/workspace/db/memory.db")` → `str(SynapseConfig.load().db_dir / "memory.db")`
- `os.path.expanduser("~/.openclaw")` or `os.path.expanduser("~/.openclaw/")` → `str(SynapseConfig.load().data_root)`
- `os.path.join(os.path.expanduser("~/.openclaw"), "workspace")` → `str(SynapseConfig.load().data_root / "workspace")`
- `os.path.join(tempfile.gettempdir(), "openclaw")` → `str(SynapseConfig.load().log_dir)`

**monitor.py specific:**
- `LOG_DIR = os.path.join(tempfile.gettempdir(), "openclaw")` → `LOG_DIR = str(SynapseConfig.load().log_dir)`
- `OPENCLAW_HOME = os.path.expanduser("~/.openclaw")` (or similar) → `SYNAPSE_HOME = str(SynapseConfig.load().data_root)` — rename the variable and update ALL usages of `OPENCLAW_HOME` in the file to `SYNAPSE_HOME`

**change_tracker.py specific:**
- The `WORKSPACE` variable and any config file list containing `"openclaw.json"` — rename list entry to `"synapse.json"`
- If a list like `["openclaw.json", ...]` exists, change `"openclaw.json"` to `"synapse.json"`

After editing all 5 files, verify no openclaw strings remain.
  </action>
  <verify>
    <automated>grep -in "openclaw" C:/Users/upayan.ghosh/personal/Synapse-OSS/workspace/change_tracker.py C:/Users/upayan.ghosh/personal/Synapse-OSS/workspace/change_viewer.py C:/Users/upayan.ghosh/personal/Synapse-OSS/workspace/finish_facts.py C:/Users/upayan.ghosh/personal/Synapse-OSS/workspace/monitor.py C:/Users/upayan.ghosh/personal/Synapse-OSS/workspace/purge_trash.py 2>/dev/null; echo "exit:$?"</automated>
  </verify>
  <done>grep returns no matches in any of the 5 files (exit code 1). All path constants use SynapseConfig.load(). monitor.py variable renamed from OPENCLAW_HOME to SYNAPSE_HOME.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep openclaw paths in workspace/sci_fi_dashboard/ module files</name>
  <files>
    workspace/sci_fi_dashboard/ingest.py
    workspace/sci_fi_dashboard/migrate_graph.py
    workspace/sci_fi_dashboard/persona.py
    workspace/sci_fi_dashboard/smart_entity.py
    workspace/sci_fi_dashboard/state.py
    workspace/sci_fi_dashboard/verify_dual_cognition.py
  </files>
  <action>
For each of these 6 files in `workspace/sci_fi_dashboard/`, apply the substitution pattern. Read each file fully before editing.

**Import addition (top of file, after existing imports) — use try/except pattern:**
```python
try:
    from synapse_config import SynapseConfig  # noqa: PLC0415
except ImportError:
    import sys as _sys
    import os as _os
    _sys.path.insert(0, _os.path.abspath(_os.path.join(_os.path.dirname(__file__), "..")))
    from synapse_config import SynapseConfig
```

**Standard substitutions (same rules as Task 1):**
- All `~/.openclaw/workspace/db/*.db` paths → `str(SynapseConfig.load().db_dir / "*.db")`
- All `~/.openclaw/` → `str(SynapseConfig.load().data_root)`
- All `~/.openclaw/workspace/sci_fi_dashboard/synapse_data` → `str(SynapseConfig.load().sbs_dir)`

**state.py specific handling:**
Find the subprocess call that invokes the openclaw binary (approximately line 87):
```python
["openclaw", "sessions", "list", "--json"]
```
Do NOT call the openclaw binary. Replace the entire subprocess block with:
```python
# TODO Phase 7: openclaw sessions list replaced by internal SQLite sessions endpoint
# sessions_raw = subprocess.run(["openclaw", "sessions", "list", "--json"], ...)
sessions_data: list = []  # Placeholder — Phase 7 populates from memory.db sessions table
```
Update any downstream code that reads `sessions_raw` to use `sessions_data` (which is an empty list).
If `subprocess` is no longer used elsewhere in `state.py` after this change, remove the `import subprocess` line.

After editing all 6 files, verify no openclaw strings remain.
  </action>
  <verify>
    <automated>grep -r "openclaw" C:/Users/upayan.ghosh/personal/Synapse-OSS/workspace/ --include="*.py" --exclude="migrate_openclaw.py" --exclude-dir="tests" --exclude-dir="__pycache__" --exclude-dir="scripts" --exclude-dir="skills" 2>/dev/null; echo "openclaw_remaining=$?"</automated>
  </verify>
  <done>grep returns no output — zero openclaw references remain in workspace/ root files and workspace/sci_fi_dashboard/ files (excluding migrate_openclaw.py and tests). state.py has TODO Phase 7 comment stub.</done>
</task>

</tasks>

<verification>
After both tasks — check scope of this plan (workspace root + sci_fi_dashboard only):

1. `grep -rn "openclaw" workspace/change_tracker.py workspace/change_viewer.py workspace/finish_facts.py workspace/monitor.py workspace/purge_trash.py` — zero results
2. `grep -rn "openclaw" workspace/sci_fi_dashboard/ingest.py workspace/sci_fi_dashboard/migrate_graph.py workspace/sci_fi_dashboard/persona.py workspace/sci_fi_dashboard/smart_entity.py workspace/sci_fi_dashboard/state.py workspace/sci_fi_dashboard/verify_dual_cognition.py` — zero results
3. `python -c "import sys; sys.path.insert(0, 'workspace'); from sci_fi_dashboard import state"` — no ImportError
4. Confirm `workspace/sci_fi_dashboard/gateway/sender.py` NOT touched by this plan (handled in Plan 03)
</verification>

<success_criteria>
- Zero openclaw references in all 11 files (5 workspace root + 6 sci_fi_dashboard)
- All files import SynapseConfig using the correct pattern for their directory depth
- state.py openclaw subprocess is stubbed with TODO Phase 7
- monitor.py OPENCLAW_HOME renamed to SYNAPSE_HOME with all usages updated
- No modification to scripts/, skills/, or gateway/sender.py (those are Plan 06 and Plan 03)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-config/01-05-SUMMARY.md`
</output>

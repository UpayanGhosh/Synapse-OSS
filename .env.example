# .env.example
# OpenClaw Environment Configuration Template

# --- LLM Provider API Keys ---
# Google Gemini (Primary brain)
GEMINI_API_KEY="your_gemini_api_key_here"

# OpenRouter (Fallback / Backup Models)
OPENROUTER_API_KEY="your_openrouter_api_key_here"

# OpenAI / Custom (For specific tools or overrides)
OPENAI_API_KEY="your_openai_api_key_here"

# Groq (Optional, used for specific high-speed transcriptions/tools)
GROQ_API_KEY="your_groq_api_key_here"

# --- Infrastructure Config ---
# (HIGHLY OPTIONAL) IP address of the local PC running Ollama inference (The Vault)
# If you leave this blank, the system will seamlessly run entirely on your Cloud API keys.
WINDOWS_PC_IP="192.168.1.xxx"
# --- Security & Webhooks ---
# Gateway internal token (Ensure this matches your Node/CLI configs)
OPENCLAW_GATEWAY_TOKEN="your_secure_gateway_token"

# WhatsApp Bridge authorization secret
WHATSAPP_BRIDGE_TOKEN="your_whatsapp_bridge_secret"

# WhatsApp Fastapi Relay Target
WHATSAPP_CHAT_URL="http://127.0.0.1:8000/chat"

# --- Operational Settings ---
# Default session type for memory (safe or spicy)
MAC_APP_SESSION_TYPE="safe"
